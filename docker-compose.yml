# Core control-plane services (Gateway + stateful dependencies).
# Provide the shared control stack for both single-node and multi-node setups.

services:
  # ============================================
  # Base Images (Debian 12)
  # ============================================
  os-base:
    env_file: config/defaults.env
    image: ${IMAGE_PREFIX:-undefined}-os-base:latest
    build:
      context: .
      dockerfile: services/common/Dockerfile.os-base
      args:
        IMAGE_PREFIX: ${IMAGE_PREFIX:-undefined}
        ROOT_CA_MOUNT_ID: root_ca
        ROOT_CA_CERT_FILENAME: ${ROOT_CA_CERT_FILENAME:-rootCA.crt}
        ROOT_CA_FINGERPRINT: ${ROOT_CA_FINGERPRINT:-}
      secrets:
        - root_ca

  python-base:
    env_file: config/defaults.env
    image: ${IMAGE_PREFIX:-undefined}-python-base:latest
    build:
      context: .
      dockerfile: services/common/Dockerfile.python-base
      args:
        IMAGE_PREFIX: ${IMAGE_PREFIX:-undefined}
        ROOT_CA_MOUNT_ID: root_ca
        ROOT_CA_CERT_FILENAME: ${ROOT_CA_CERT_FILENAME:-rootCA.crt}
        ROOT_CA_FINGERPRINT: ${ROOT_CA_FINGERPRINT:-}
      secrets:
        - root_ca

  # ============================================
  # Permission Helper (for RustFS)
  # ============================================
  permission-helper:
    env_file: config/defaults.env
    image: alpine
    container_name: ${PROJECT_NAME:-undefined}-permission-helper
    volumes:
      - rustfs_data:/data/rustfs
    command: >
      sh -c "
        chown -R 10001:10001 /data/rustfs 2>/dev/null || true && echo 'RustFS permissions fixed'
      "
    restart: "no"

  # ============================================
  # RustFS (S3 Compatible Storage)
  # ============================================
  s3-storage:
    env_file: config/defaults.env
    image: rustfs/rustfs:latest
    container_name: ${PROJECT_NAME:-undefined}-s3-storage
    depends_on:
      permission-helper:
        condition: service_completed_successfully
    ports:
      - "${PORT_S3:-9000}:9000" # S3 API
      - "${PORT_S3_MGMT:-9001}:9001" # Console
    volumes:
      - rustfs_data:/data/rustfs
      - ./config/lifecycle.yml:/app/config/lifecycle.yml:ro
    environment:
      - RUSTFS_VOLUMES=/data/rustfs
      - RUSTFS_ADDRESS=0.0.0.0:9000
      - RUSTFS_ACCESS_KEY=${RUSTFS_ACCESS_KEY:-}
      - RUSTFS_SECRET_KEY=${RUSTFS_SECRET_KEY:-}
      - RUSTFS_CONSOLE_ENABLE=true
      - RUSTFS_CONSOLE_ADDRESS=0.0.0.0:9001
      - RUSTFS_DEDUPLICATION=${RUSTFS_DEDUPLICATION:-true}
      - RUSTFS_COMPRESSION=${RUSTFS_COMPRESSION:-auto}
      - RUSTFS_LIFECYCLE_POLICY_PATH=/app/config/lifecycle.yml
      - HTTP_PROXY
      - HTTPS_PROXY
      - NO_PROXY
      - http_proxy
      - https_proxy
      - no_proxy
    healthcheck:
      test: [ "CMD", "sh", "-c", "curl -f --noproxy '*' http://127.0.0.1:9000/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      external_network: {}


  # ============================================
  # ScyllaDB (DynamoDB Compatible DB)
  # ============================================
  database:
    env_file: config/defaults.env
    image: scylladb/scylla:latest
    container_name: ${PROJECT_NAME:-undefined}-database
    ports:
      - "${PORT_DATABASE:-0}:8000" # Alternator API (Host dynamic -> Container 8000)
    volumes:
      - scylladb_data:/var/lib/scylla
    command: --smp 1 --memory ${SCYLLADB_MEMORY:-1}G --developer-mode 1 --alternator-port 8000 --alternator-address 0.0.0.0 --alternator-write-isolation always_use_lwt
    environment:
      - HTTP_PROXY
      - HTTPS_PROXY
      - NO_PROXY
      - http_proxy
      - https_proxy
      - no_proxy
    healthcheck:
      test: [ "CMD-SHELL", "cqlsh -e 'DESCRIBE KEYSPACES' || exit 1" ]
      interval: 10s
      timeout: 10s
      retries: 30
      start_period: 180s
    restart: unless-stopped
    networks:
      external_network: {}


  # ============================================
  # VictoriaLogs
  # ============================================
  victorialogs:
    env_file: config/defaults.env
    image: victoriametrics/victoria-logs:latest
    container_name: ${PROJECT_NAME:-undefined}-victorialogs
    ports:
      - "${PORT_VICTORIALOGS:-0}:9428" # VictoriaLogs UI/API
    volumes:
      - victorialogs_data:/victoria-logs-data
    command:
      - "-storageDataPath=/victoria-logs-data"
      - "-retentionPeriod=7d"
    environment:
      - HTTP_PROXY
      - HTTPS_PROXY
      - NO_PROXY
      - http_proxy
      - https_proxy
      - no_proxy
    healthcheck:
      test: [ "CMD", "wget", "-q", "-Y", "off", "--spider", "http://127.0.0.1:9428/health" ]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    networks:
      external_network: {}


  # ============================================
  # API Gateway (Facade)
  # ============================================
  gateway:
    env_file: config/defaults.env
    image: ${IMAGE_PREFIX:-undefined}-gateway:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
      additional_contexts:
        config: ${CONFIG_DIR:-services/gateway/config}
      args:
        IMAGE_PREFIX: ${IMAGE_PREFIX:-undefined}
        SERVICE_USER: ${PROJECT_NAME:-undefined}
        SERVICE_UID: ${RUN_UID:-1000}
        SERVICE_GID: ${RUN_GID:-1000}
    container_name: ${PROJECT_NAME:-undefined}-gateway
    # NOTE: ports and networks are defined by adapter files (fc.yml / containerd.yml)
    volumes:
      - ${CERT_DIR:-~/.esb/certs}:/app/config/ssl:ro
    environment:
      # --- Required / Core Authentication (auto-generated if unset) ---
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-}
      - X_API_KEY=${X_API_KEY:-}
      - AUTH_USER=${AUTH_USER:-}
      - AUTH_PASS=${AUTH_PASS:-}
      - RUSTFS_ACCESS_KEY=${RUSTFS_ACCESS_KEY:-}
      - RUSTFS_SECRET_KEY=${RUSTFS_SECRET_KEY:-}

      # --- Server & Network Configuration ---
      - UVICORN_BIND_ADDR=0.0.0.0:8443
      - ENABLE_SSL=true
      - SSL_CERT_PATH=/app/config/ssl/server.crt
      - SSL_KEY_PATH=/app/config/ssl/server.key
      - CONTAINERS_NETWORK=${NETWORK_EXTERNAL:-${PROJECT_NAME:-undefined}-external}
      - AUTH_ENDPOINT_PATH
      - PROJECT_NAME=${PROJECT_NAME:-undefined}
      - IMAGE_TAG=${IMAGE_TAG:-}
      - FUNCTION_IMAGE_PREFIX=${FUNCTION_IMAGE_PREFIX:-}

      # --- Tuning & Resilience (Pass-through) ---
      - AGENT_INVOKE_PROXY
      - LAMBDA_INVOKE_TIMEOUT
      - CIRCUIT_BREAKER_THRESHOLD
      - CIRCUIT_BREAKER_RECOVERY_TIMEOUT

      # --- Observability ---
      - LOG_LEVEL
      - LOG_PAYLOADS
      - PYTHONUNBUFFERED=1
      - VICTORIALOGS_URL=${VICTORIALOGS_URL:-http://victorialogs:9428}  # Gateway -> VictoriaLogs

      # --- Auto-Scaling ---
      - DEFAULT_MAX_CAPACITY=${DEFAULT_MAX_CAPACITY:-1}
      - DEFAULT_MIN_CAPACITY=${DEFAULT_MIN_CAPACITY:-0}
      - POOL_ACQUIRE_TIMEOUT=${POOL_ACQUIRE_TIMEOUT:-5.0}
      - HEARTBEAT_INTERVAL=${HEARTBEAT_INTERVAL:-30}
      - GATEWAY_IDLE_TIMEOUT_SECONDS=${GATEWAY_IDLE_TIMEOUT_SECONDS:-300}
      - ENABLE_CONTAINER_PAUSE=${ENABLE_CONTAINER_PAUSE:-false}
      - PAUSE_IDLE_SECONDS=${PAUSE_IDLE_SECONDS:-30}

      # --- System / Proxy ---
      - HTTP_PROXY
      - HTTPS_PROXY
      - NO_PROXY
      - http_proxy
      - https_proxy
      - no_proxy
      - GRPC_VERBOSITY=ERROR
    depends_on:
      database:
        condition: service_healthy
      s3-storage:
        condition: service_healthy
      victorialogs:
        condition: service_healthy
    restart: unless-stopped

volumes:
  scylladb_data:
  rustfs_data:
  victorialogs_data:

secrets:
  root_ca:
    file: ${CERT_DIR:-~/.esb/certs}/rootCA.crt

networks:
  # Public network (exposed ports).
  external_network:
    # Let Docker choose the subnet unless the network is pre-created.
    name: ${NETWORK_EXTERNAL:-${PROJECT_NAME:-undefined}-external}
    driver: bridge
