"""Legacy Python CLI helpers (formerly tools/cli).

This package hosts the remaining Python-powered node management helpers while the
primary `esb` surface routes through the Go binary in `cli/cmd/esb`.
"""
